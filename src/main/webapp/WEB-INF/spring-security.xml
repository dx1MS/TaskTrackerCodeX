<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">


    <!--<jdbc:database id="dataSource" type="MySql">-->
        <!--<jdbc:script location="classpath:db-schema.sql"/>-->
        <!--<jdbc:script location="classpath:db-test-data.sql"/>-->
    <!--</jdbc:database>-->
    <!-- enable use-expressions -->
    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/projects**" access="hasRole('MANAGER')" />

        <!-- access denied page -->
        <access-denied-handler error-page="/403" />

        <form-login
                login-page="/login"
                default-target-url="/projects"
                authentication-failure-url="/login?error"
                username-parameter="username"
                password-parameter="password" />
        <logout logout-success-url="/login?logout"  />
        <!-- enable csrf protection -->
        <csrf/>
    </http>

    <!--authorities-by-username-query = "select u.user_name, r.role_name from roles r, user u where u.USER_ROLE_ID=r.ROLE_ID and u.user_name =?  " /-->


    <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query = "select user_name, password, enabled from user where user_name=?"
                               authorities-by-username-query = "select u.user_name, u.role from user u where u.user_name =?" />
                               <!--authorities-by-username-query=-->
                                       <!--"select user_name, role from user_roles where username =?  " />-->
        </authentication-provider>
    </authentication-manager>
</beans:beans>